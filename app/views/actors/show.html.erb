<%= link_to 'Actors', actors_path%>
<h2><%= @actor.name %></h2>
<p>Description: <%= @actor.description %></p>
<p>Roles:</p>
<ul>
  <% @actor.characters.each do |character| %>
    <li><%= link_to character.character_name, character_path(character) %></li>
  <%end%>
</ul>
